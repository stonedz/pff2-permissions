moduleConf:
  # User class name
  userClass: AnagraficaBusiness

  # Session key to get Id of the currently logged user
  sessionUserId: id_user

  # Method on the user instance to get the Permission instance (POPO)
  getPermission: getPermesso

  # Controller to redirect if not logged
  controllerNotLogged: Index

  # Action to redirect if not logged
  actionNotLogged: index

  # Name of the class used as Permission
  permissionClass: Permesso
